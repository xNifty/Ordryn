openapi: 3.0.3
info:
  title: Ordryn / GoTodo API
  description: |
    JSON API for Ordryn (GoTodo). Self-host with `GOTODO_MODE=full` (UI + API) or
    `GOTODO_MODE=api` (API only). Redis is required for `/api/v1` authentication,
    rate limiting, and live SSE fan-out.

    Auth: session cookie (SPA login/register) or `Authorization: Bearer <api_key>`.

    Rate limits (per user, Redis token bucket). Reads and writes are independent.
    Bearer API keys: 120 GET/min (~2/sec) and 60 writes/min (~1/sec).
    SPA session cookies use a higher budget (600 GET/min, 240 writes/min) so the
    web UI is not gated by the external API ceiling.

    Deprecations: task favoriting (`favorite`) is deprecated and will be removed
    in API v2. Create, update, reorder (`favorite: true`), and CSV import that
    use `favorite` still succeed and return `Deprecation: true`, a RFC 7234
    `Warning` header, and `deprecation_notice` in the JSON body.
  version: 1.0.0
  license:
    name: See LICENSE in repository

servers:
  - url: /
    description: Same-origin (BASE_PATH may prefix these paths)

tags:
  - name: Health
  - name: Auth
  - name: Profile
  - name: API Keys
  - name: Tasks
  - name: Notifications
  - name: Projects
  - name: Tags
  - name: Saved Views
  - name: Device Auth
  - name: GitHub
  - name: Live
  - name: Images

paths:
  /api/v1/health:
    get:
      tags: [Health]
      summary: Public health probe
      security: []
      responses:
        "200":
          description: Health snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"

  /api/v1/site:
    get:
      tags: [Health]
      summary: Public site metadata for the SPA shell
      security: []
      responses:
        "200":
          description: Site metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteInfo"

  /api/v1/events:
    get:
      tags: [Live]
      summary: Live task and project updates (Server-Sent Events)
      description: |
        Long-lived `text/event-stream` for the authenticated user. The SPA uses a
        session cookie (`EventSource` cannot send `Authorization` headers).

        Named events:
        - `ready` — stream is open
        - `ping` — keepalive about every 25s
        - `task-update` — JSON invalidation payload (`type`, optional `task_id` /
          `project_id`, `actor_id`, `timestamp`). Clients should refetch the
          current view; the payload does not include task bodies.

        Types include `task.created`, `task.updated`, `task.deleted`,
        `task.reordered`, `project.updated`, and `join.request`.
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Event stream
          content:
            text/event-stream:
              schema:
                type: string
                example: |
                  event: ready
                  data: {}

                  event: task-update
                  data: {"type":"task.updated","task_id":42,"project_id":7,"actor_id":3}
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/register:
    post:
      tags: [Auth]
      summary: Register and start a session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: User created; Set-Cookie session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/APIError"
        "429":
          $ref: "#/components/responses/RateLimited"

  /api/v1/join-requests:
    post:
      tags: [Auth]
      summary: Submit a request to join the site
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
                message:
                  type: string
                  maxLength: 500
      responses:
        "200":
          description: Acknowledgement (same message even if already pending)
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/APIError"
        "429":
          $ref: "#/components/responses/RateLimited"

  /api/v1/auth/login:
    post:
      tags: [Auth]
      summary: Login and start a session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Authenticated with a session cookie, or MFA challenge (pending cookie only)
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/User"
                  - $ref: "#/components/schemas/MFARequired"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"

  /api/v1/auth/mfa/verify:
    post:
      tags: [Auth]
      summary: Complete MFA login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFACodeRequest"
      responses:
        "200":
          description: Authenticated; Set-Cookie session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"

  /api/v1/auth/logout:
    post:
      tags: [Auth]
      summary: Clear session cookie
      security: []
      responses:
        "200":
          description: Logged out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"

  /api/v1/auth/username-available:
    get:
      tags: [Auth]
      summary: Check whether a username is available
      security: []
      parameters:
        - name: username
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Availability result
          content:
            application/json:
              schema:
                type: object
                properties:
                  username:
                    type: string
                  available:
                    type: boolean
                  valid:
                    type: boolean
                  message:
                    type: string

  /api/v1/auth/forgot-password:
    post:
      tags: [Auth]
      summary: Request a password reset email
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, confirm_email]
              properties:
                email:
                  type: string
                  format: email
                confirm_email:
                  type: string
                  format: email
      responses:
        "200":
          description: Request accepted (always returns ok when valid)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          $ref: "#/components/responses/RateLimited"

  /api/v1/auth/reset-password:
    get:
      tags: [Auth]
      summary: Validate a password reset token
      security: []
      parameters:
        - name: token
          in: query
          required: true
          schema:
            type: string
        - name: id
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Token is valid
          content:
            application/json:
              schema:
                type: object
                properties:
                  valid:
                    type: boolean
                  email:
                    type: string
                    format: email
        "400":
          $ref: "#/components/responses/BadRequest"
    post:
      tags: [Auth]
      summary: Set a new password using a reset token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id, token, new_password, confirm_password]
              properties:
                id:
                  type: string
                token:
                  type: string
                new_password:
                  type: string
                  format: password
                confirm_password:
                  type: string
                  format: password
      responses:
        "200":
          description: Password updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/auth/device/code:
    post:
      tags: [Device Auth]
      summary: Start device authorization (Android SSO)
      description: |
        Optional `redirect_uri` binds a native deep link (allowlisted `ordryn://auth-complete`)
        to this authorization. After the user approves or denies in the browser, the SPA
        navigates to that URI so the Android app can resume.
      security: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                client_name:
                  type: string
                redirect_uri:
                  type: string
                  description: Optional native deep link; must be ordryn://auth-complete
                  example: ordryn://auth-complete
      responses:
        "200":
          description: Device codes issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceCodeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/auth/device/token:
    post:
      tags: [Device Auth]
      summary: Poll device authorization for API key
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [device_code, grant_type]
              properties:
                device_code:
                  type: string
                grant_type:
                  type: string
                  description: Must be urn:ietf:params:oauth:grant-type:device_code
      responses:
        "200":
          description: Approved; returns api_key once
          content:
            application/json:
              schema:
                type: object
                properties:
                  api_key:
                    type: string
                  key_prefix:
                    type: string
                  key_name:
                    type: string
        "400":
          description: Pending, denied, expired, or invalid (OAuth-style error body)

  /api/v1/auth/device/status:
    get:
      tags: [Device Auth]
      summary: Look up a pending device authorization by user_code
      security: []
      parameters:
        - name: user_code
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Device auth status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceStatus"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/auth/device/approve:
    post:
      tags: [Device Auth]
      summary: Approve a pending device authorization
      security:
        - SessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_code]
              properties:
                user_code:
                  type: string
      responses:
        "200":
          description: Approved; includes redirect_uri when the device requested one
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceDecisionResponse"

  /api/v1/auth/device/deny:
    post:
      tags: [Device Auth]
      summary: Deny a pending device authorization
      security:
        - SessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_code]
              properties:
                user_code:
                  type: string
      responses:
        "200":
          description: Denied; includes redirect_uri when the device requested one
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceDecisionResponse"

  /api/v1/me:
    get:
      tags: [Profile]
      summary: Current user profile
      description: |
        Unauthenticated session probes return HTTP 200 with a JSON `null` body
        (not 401), so SPA bootstrap works behind proxies that rewrite 401 to HTML.
        Invalid Bearer tokens still return 401.
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Profile, or null when no session
          content:
            application/json:
              schema:
                nullable: true
                allOf:
                  - $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      tags: [Profile]
      summary: Update profile fields
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserPatch"
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/me/username:
    post:
      tags: [Profile]
      summary: One-time username claim for migrated accounts
      description: |
        Allowed only when `username_change_available` is true. Sets the username
        and clears the free-change flag. New registrations do not get this flag.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_name]
              properties:
                user_name:
                  type: string
                  minLength: 3
                  maxLength: 32
                  pattern: '^[A-Za-z0-9_]+$'
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/APIError"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/me/avatar:
    post:
      tags: [Profile]
      summary: Upload profile picture
      description: |
        Uploads and updates the user's profile picture. Image hosting must be
        configured. Accepts PNG and JPEG images only, bounded by the site's upload limit.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Updated profile with new avatar URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "413":
          description: Image exceeds maximum allowed size
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIError"
    delete:
      tags: [Profile]
      summary: Remove profile picture
      description: Clears the user's avatar URL.
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Updated profile with cleared avatar URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/me/password:
    post:
      tags: [Profile]
      summary: Change password
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangePasswordRequest"
      responses:
        "200":
          description: Password changed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/me/mfa:
    get:
      tags: [Profile]
      summary: MFA status
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Whether MFA is enabled and unused recovery codes remaining
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFAStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/me/mfa/setup:
    post:
      tags: [Profile]
      summary: Start MFA enrollment
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: TOTP secret and otpauth URL (secret stashed on the session)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFASetup"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/me/mfa/enable:
    post:
      tags: [Profile]
      summary: Confirm TOTP and enable MFA
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFACodeRequest"
      responses:
        "200":
          description: MFA enabled; recovery codes shown once
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFARecoveryCodes"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/me/mfa/disable:
    post:
      tags: [Profile]
      summary: Disable MFA
      description: Requires a current TOTP code or an unused recovery code.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFACodeRequest"
      responses:
        "200":
          description: MFA disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/me/mfa/recovery-codes:
    post:
      tags: [Profile]
      summary: Replace recovery codes
      description: Requires a current TOTP code or an unused recovery code. New codes are shown once.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFACodeRequest"
      responses:
        "200":
          description: New recovery codes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFARecoveryCodes"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/users/search:
    get:
      tags: [Projects]
      summary: Search usernames for project invites or discussion mentions
      description: |
        Case-insensitive username prefix match. Empty `q` or queries shorter than
        2 characters return `[]`. Does not return emails. Rate limited more tightly
        than other read endpoints.

        Without `project_id`, this is the invite picker: it excludes the current
        user, banned users, and users who opted out of project invites.

        With `project_id`, results are limited to members of that project (including
        the current user). The caller must be able to access the project. Invite
        opt-out does not apply; banned members are still excluded.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
        - name: project_id
          in: query
          required: false
          description: When set, only matching members of this project are returned.
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Matching usernames
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserSearchHit"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      tags: [GitHub]
      summary: GitHub connection status for the current user
      responses:
        "200":
          description: Connection status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GitHubConnection"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      tags: [GitHub]
      summary: Disconnect GitHub
      responses:
        "200":
          description: Disconnected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/me/github/pat:
    post:
      tags: [GitHub]
      summary: Connect GitHub with a personal access token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token]
              properties:
                token:
                  type: string
      responses:
        "200":
          description: Connected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GitHubConnection"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/APIError"

  /api/v1/me/github/oauth/start:
    get:
      tags: [GitHub]
      summary: Start GitHub OAuth (requires admin-configured OAuth App)
      responses:
        "200":
          description: Authorize URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  authorize_url:
                    type: string
                  redirect_uri:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/APIError"

  /api/v1/auth/github/callback:
    get:
      tags: [GitHub]
      summary: GitHub OAuth callback (public redirect)
      security: []
      parameters:
        - name: code
          in: query
          schema:
            type: string
        - name: state
          in: query
          schema:
            type: string
      responses:
        "303":
          description: Redirects to Settings

  /api/v1/webhooks/github:
    post:
      tags: [GitHub]
      summary: GitHub issues webhook (updates linked tasks only; never creates tasks)
      security: []
      parameters:
        - name: X-Ordryn-Webhook-Secret
          in: header
          schema:
            type: string
        - name: X-Hub-Signature-256
          in: header
          schema:
            type: string
        - name: X-GitHub-Event
          in: header
          schema:
            type: string
      responses:
        "200":
          description: Accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/APIError"

  /api/v1/api-keys:
    get:
      tags: [API Keys]
      summary: List active API keys (no plaintext)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Key metadata
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/APIKey"
    post:
      tags: [API Keys]
      summary: Create or rotate a named API key
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  maxLength: 80
      responses:
        "201":
          description: Key created; plaintext shown once
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIKeyCreated"
        "400":
          $ref: "#/components/responses/BadRequest"
        "503":
          $ref: "#/components/responses/APIError"

  /api/v1/api-keys/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    patch:
      tags: [API Keys]
      summary: Rename an API key
      description: Changes only the display name. The secret token is unchanged. Names are unique among the user's active keys (case-insensitive).
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  maxLength: 80
      responses:
        "200":
          description: Updated key metadata (no plaintext)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIKey"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [API Keys]
      summary: Revoke an API key
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Revoked
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks:
    get:
      tags: [Tasks]
      summary: List tasks
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            minimum: 1
        - name: per_page
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: status
          in: query
          schema:
            type: string
        - name: project
          in: query
          schema:
            type: string
          description: Project id, or none/0 for no project
        - name: tag
          in: query
          schema:
            type: string
          description: >
            Tag id when filtering a specific project, or a tag name (or id resolved to name)
            when listing all tasks / inbox.
        - name: due
          in: query
          schema:
            type: string
        - name: priority
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
        - name: completed
          in: query
          schema:
            type: string
        - name: workflow_claim_scope
          in: query
          schema:
            type: string
            enum: [mine, all]
          description: >
            When mine, hide unclaimed kanban-project tasks (classic tasks unchanged).
            Use all for the kanban board backlog. Default is no claim filter when omitted.
        - name: include_subtasks
          in: query
          schema:
            type: string
            enum: ["1", "true", "yes"]
          description: >
            When set, matching subtasks are returned as their own rows instead of
            only root tasks. Used by dashboard due sections so leftover overdue
            children stay visible after a parent is completed.
        - name: sprint_id
          in: query
          schema:
            type: string
          description: >
            Filter kanban tasks by sprint id. Use none, backlog, or 0 for unassigned
            (backlog) tasks. Omitted means no sprint filter.
      responses:
        "200":
          description: Paginated task list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskList"
    post:
      tags: [Tasks]
      summary: Create a task
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskCreate"
      responses:
        "201":
          description: >
            Created. When the request included `favorite`, the response also has
            `Deprecation` and `Warning` headers and `deprecation_notice` in the body.
          headers:
            Deprecation:
              $ref: "#/components/headers/FavoriteDeprecation"
            Warning:
              $ref: "#/components/headers/FavoriteWarning"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/tasks/reorder:
    post:
      tags: [Tasks]
      summary: Reorder tasks (favorite grouping is deprecated)
      description: |
        Updates manual sort order (`position`) within one favorite or non-favorite
        group. Favorite grouping is deprecated and will be removed in API v2; the
        `favorite` field is still required. Reordering the starred group
        (`favorite: true`) returns `Deprecation` and `Warning` headers plus
        `deprecation_notice` in the JSON body.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskReorder"
      responses:
        "200":
          description: >
            Reordered. When `favorite` is true, the response also has `Deprecation`
            and `Warning` headers and `deprecation_notice` in the body.
          headers:
            Deprecation:
              $ref: "#/components/headers/FavoriteDeprecation"
            Warning:
              $ref: "#/components/headers/FavoriteWarning"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskReorderResult"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/tasks/bulk:
    post:
      tags: [Tasks]
      summary: Apply a bulk action to tasks
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskBulk"
      responses:
        "200":
          description: Action applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkResult"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/tasks/undo:
    post:
      tags: [Tasks]
      summary: Restore deleted tasks via undo_token (or session undo buffer)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                undo_token:
                  type: string
      responses:
        "200":
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  restored:
                    type: integer
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/tasks/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Tasks]
      summary: Get one task
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Tasks]
      summary: Update a task
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskPatch"
      responses:
        "200":
          description: >
            Updated task. When the request included `favorite`, the response also has
            `Deprecation` and `Warning` headers and `deprecation_notice` in the body.
          headers:
            Deprecation:
              $ref: "#/components/headers/FavoriteDeprecation"
            Warning:
              $ref: "#/components/headers/FavoriteWarning"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Tasks]
      summary: Delete a task (returns undo_token)
      description: |
        When the task has subtasks, pass mode=cascade (default) to delete them too,
        or mode=reparent with new_parent_id (0 or omit to promote to root) to move
        children before deleting the parent.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: mode
          in: query
          schema:
            type: string
            enum: [cascade, reparent]
            default: cascade
        - name: new_parent_id
          in: query
          schema:
            type: integer
          description: Target root task id when mode=reparent; 0 or omit promotes children to root
      responses:
        "200":
          description: Deleted with undo token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks/{id}/events:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Tasks]
      summary: Task activity timeline
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Events newest-first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TaskEvent"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects:
    get:
      tags: [Projects]
      summary: List projects
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Projects (active owned first by position, then shared, then archived)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Project"
    post:
      tags: [Projects]
      summary: Create a project
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectCreate"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/projects/reorder:
    post:
      tags: [Projects]
      summary: Reorder owned projects
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [project_ids]
              properties:
                project_ids:
                  type: array
                  items:
                    type: integer
                  description: Full ordered list of active (non-archived) project IDs owned by the caller
      responses:
        "200":
          description: Reordered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/projects/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Projects]
      summary: Get a project (owned or shared)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Projects]
      summary: Update project name, description, and/or workflow mode (owner only)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectPatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Cannot disable kanban while the project has tasks
    delete:
      tags: [Projects]
      summary: Delete a project (owner only)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Deleted
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/archive:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Projects]
      summary: Archive a project (owner only)
      description: >
        Moves the project out of active lists, applies the protected archived tag to
        its tasks, and prevents new tasks from being added. Existing tasks remain.
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/restore:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Projects]
      summary: Restore an archived project (owner only)
      description: >
        Returns the project to active lists, removes the protected archived tag from
        its tasks, and allows new tasks again.
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Restored
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/github:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [GitHub]
      summary: Get linked GitHub repository for a project
      responses:
        "200":
          description: Link status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectGitHubRepo"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [GitHub]
      summary: Link a GitHub repository (owner; requires admin on the repo)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [repository]
              properties:
                repository:
                  type: string
                  description: owner/repo or GitHub URL
      responses:
        "200":
          description: Linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectGitHubRepo"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/APIError"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [GitHub]
      summary: Unlink GitHub repository (clears task issue links; does not delete GH issues)
      responses:
        "200":
          description: Unlinked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "403":
          $ref: "#/components/responses/APIError"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/statuses:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Projects]
      summary: List kanban statuses for a project
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Status columns (empty when project is classic)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectStatus"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Projects]
      summary: Create a status column (owner, kanban only, max 8)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  maxLength: 40
                is_done:
                  type: boolean
                is_default:
                  type: boolean
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectStatus"
        "409":
          description: Status limit or name conflict

  /api/v1/projects/{id}/statuses/reorder:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Projects]
      summary: Reorder status columns (owner, kanban only)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status_ids]
              properties:
                status_ids:
                  type: array
                  items:
                    type: integer
      responses:
        "200":
          description: Reordered
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean

  /api/v1/projects/{id}/statuses/{statusId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: statusId
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags: [Projects]
      summary: Update a status column (owner)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                is_done:
                  type: boolean
                is_default:
                  type: boolean
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectStatus"
    delete:
      tags: [Projects]
      summary: Delete a status column (owner)
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: move_to_status_id
          in: query
          schema:
            type: integer
          description: Required when tasks remain in the column
      responses:
        "204":
          description: Deleted

  /api/v1/projects/{id}/sprints:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Projects]
      summary: List sprints for a kanban project
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Sprints (empty when project is classic)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectSprint"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Projects]
      summary: Create a sprint (owner, kanban only, max 50)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  maxLength: 60
                description:
                  type: string
                  maxLength: 80
                  description: Optional short goal shown with the sprint name (e.g. "features required for v3.0.0 release")
                start_date:
                  type: string
                  format: date
                  description: Inclusive start (YYYY-MM-DD). Leave blank or omit for a dateless sprint (e.g. Icebox). For dated sprints, must not overlap another sprint in this project.
                end_date:
                  type: string
                  format: date
                  description: Inclusive end (YYYY-MM-DD). Leave blank or omit for a dateless sprint. Adjacent ranges (next day after another sprint ends) are allowed.
                lock_date:
                  type: string
                  format: date
                  nullable: true
                  description: Optional. From this UTC date onward, only the project owner can add tasks to the sprint. Cannot be set on dateless sprints. Omit or null to leave the sprint unlocked.
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectSprint"
        "400":
          description: Invalid dates, inverted range, or overlap with another sprint
        "409":
          description: Sprint limit or name conflict

  /api/v1/projects/{id}/sprints/{sprintId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: sprintId
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags: [Projects]
      summary: Update a sprint (owner)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 60
                description:
                  type: string
                  maxLength: 80
                  description: Optional short goal shown with the sprint name. Empty string clears it.
                start_date:
                  type: string
                  format: date
                  description: Inclusive start (YYYY-MM-DD). Must not overlap another sprint in this project.
                end_date:
                  type: string
                  format: date
                  description: Inclusive end (YYYY-MM-DD). Adjacent ranges are allowed.
                lock_date:
                  type: string
                  format: date
                  nullable: true
                  description: Optional. From this UTC date onward, only the project owner can add tasks. Null or empty clears the lock date.
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectSprint"
    delete:
      tags: [Projects]
      summary: Delete a sprint (owner). Tasks go to backlog unless moved.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: move_to_sprint_id
          in: query
          schema:
            type: integer
          description: Optional destination sprint; 0 or omit leaves tasks in the backlog
      responses:
        "204":
          description: Deleted

  /api/v1/projects/{id}/sprints/backlog:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Projects]
      summary: Get the backlog system sprint for a kanban project
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Backlog system sprint details
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                    example: 0
                  project_id:
                    type: integer
                  name:
                    type: string
                    example: "Backlog"
                  description:
                    type: string
                    maxLength: 80
                    example: "Cold storage for future features"
                  is_active:
                    type: boolean
                    example: false
                  is_locked:
                    type: boolean
                    example: false
                  is_system:
                    type: boolean
                    example: true
                  task_count:
                    type: integer
        "400":
          description: Project is not in kanban mode
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Projects]
      summary: Update the backlog system sprint (owner)
      description: Updates the backlog system sprint name and/or description. Cannot set start, end, or lock dates.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 60
                  description: New name for the backlog system sprint
                description:
                  type: string
                  maxLength: 80
                  description: New description for the backlog system sprint
      responses:
        "200":
          description: Backlog system sprint updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                    example: 0
                  project_id:
                    type: integer
                  name:
                    type: string
                  description:
                    type: string
                  is_active:
                    type: boolean
                  is_locked:
                    type: boolean
                  is_system:
                    type: boolean
                  task_count:
                    type: integer
        "400":
          description: Invalid request or attempt to set dates
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks/{id}/github-issue:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [GitHub]
      summary: Create a GitHub issue from this task and link it
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                body:
                  type: string
      responses:
        "201":
          description: Issue created and linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskGitHubIssue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/APIError"
    put:
      tags: [GitHub]
      summary: Link an existing GitHub issue (never creates an Ordryn task)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [issue]
              properties:
                issue:
                  type: string
                  description: Issue number or GitHub issue URL in the linked repo
      responses:
        "200":
          description: Linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskGitHubIssue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/APIError"
    delete:
      tags: [GitHub]
      summary: Unlink GitHub issue from task (does not delete the issue on GitHub)
      responses:
        "200":
          description: Unlinked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks/{id}/claim:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Tasks]
      summary: Claim a kanban task (or take over from another claimer)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Tasks]
      summary: Release claim on a kanban task
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/notifications:
    get:
      tags: [Notifications]
      summary: List in-app notifications
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            minimum: 1
        - name: per_page
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Notification list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationList"

  /api/v1/notifications/unread-count:
    get:
      tags: [Notifications]
      summary: Unread notification count
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Count
          content:
            application/json:
              schema:
                type: object
                properties:
                  unread_count:
                    type: integer

  /api/v1/notifications/read-all:
    post:
      tags: [Notifications]
      summary: Mark all notifications read
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Marked read

  /api/v1/notifications/{id}/read:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Notifications]
      summary: Mark one notification read
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Marked read
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks/{id}/time-entries:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Tasks]
      summary: List time entries for a kanban task
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Time entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TaskTimeEntry"
    post:
      tags: [Tasks]
      summary: Add a time entry
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [minutes]
              properties:
                minutes:
                  type: integer
                  minimum: 1
                  maximum: 1440
                note:
                  type: string
                  maxLength: 200
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskTimeEntry"

  /api/v1/tasks/{id}/comments:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Tasks]
      summary: List discussion comments for a project task
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Comments oldest-first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TaskComment"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Tasks]
      summary: Add a discussion comment (any project member, including viewers)
      description: |
        `@username` mentions matching current project members notify those users
        (`task_mentioned`). Other members still receive `task_commented`.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
                  minLength: 1
                  maxLength: 2000
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskComment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks/{id}/comments/{commentId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: commentId
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags: [Tasks]
      summary: Edit a discussion comment (author or project owner)
      description: |
        Authors may edit their own comments. Project owners may edit any comment
        on the task. Previous content is stored in an audit log.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
                  minLength: 1
                  maxLength: 2000
      responses:
        "200":
          description: Updated comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskComment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    delete:
      tags: [Tasks]
      summary: Soft-delete a comment (author or project owner)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Deleted
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/tasks/{id}/comments/{commentId}/revisions:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: commentId
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [Tasks]
      summary: List previous versions of a comment (project owner or site admin)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Revisions newest-first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TaskCommentRevision"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks/{id}/comments/{commentId}/restore:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: commentId
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [Tasks]
      summary: Restore a previous comment version (project owner or site admin)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [revision_id]
              properties:
                revision_id:
                  type: integer
      responses:
        "200":
          description: Restored comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskComment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tasks/{id}/time-entries/{entryId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: entryId
        in: path
        required: true
        schema:
          type: integer
    delete:
      tags: [Tasks]
      summary: Delete a time entry (author or project owner)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Deleted

  /api/v1/projects/{id}/members:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Projects]
      summary: List project members
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectMember"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/members/{userId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: userId
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags: [Projects]
      summary: Update member role (owner only)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RoleBody"
      responses:
        "204":
          description: Updated
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Projects]
      summary: Remove member or leave project
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Removed
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/invites:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Projects]
      summary: List pending project invites
      description: Any project member can list pending invites. Creating and revoking invites remains owner-only.
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Invites
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectInvite"
    post:
      tags: [Projects]
      summary: Invite a user by username (owner only)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectInviteCreate"
      responses:
        "200":
          description: Invite created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectInvite"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Project not found, or username not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /api/v1/projects/{id}/invites/{inviteId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: inviteId
        in: path
        required: true
        schema:
          type: integer
    delete:
      tags: [Projects]
      summary: Revoke a pending invite (owner only)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Revoked
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/events:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Projects]
      summary: Project settings activity
      description: Sharing, membership, status, and other project-settings events. Board/task activity is not included; use GET /api/v1/tasks/{id}/events for a task timeline.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            maximum: 200
      responses:
        "200":
          description: Project settings and sharing events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectEvent"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/project-invites:
    get:
      tags: [Projects]
      summary: List pending invites for the current user
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Pending invites
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectInvite"

  /api/v1/project-invites/{id}/accept:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Projects]
      summary: Accept a project invite
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Accepted
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/project-invites/{id}/decline:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Projects]
      summary: Decline a project invite
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Declined
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/share-links:
    get:
      tags: [Projects]
      summary: List share links for a scope
      description: Project members can list all share links for that project.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: scope_type
          in: query
          required: true
          schema:
            type: string
            enum: [project]
        - name: scope_id
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Share links
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ShareLink"
    post:
      tags: [Projects]
      summary: Create a read-only share link
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShareLinkCreate"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShareLink"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/share-links/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    delete:
      tags: [Projects]
      summary: Revoke a share link
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Revoked
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/share-links/view/{token}:
    parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
    get:
      tags: [Projects]
      summary: Public read-only task list for a share link
      responses:
        "200":
          description: Shared tasks
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShareLinkView"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tags:
    get:
      tags: [Tags]
      summary: List tags
      description: >
        Omit project_id to list personal tags plus tags on accessible projects.
        project_id=0 lists personal (inbox) tags. A positive project_id lists that project's tags.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: project_id
          in: query
          schema:
            type: integer
          description: 0 for personal tags, or a project id
      responses:
        "200":
          description: Tags
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Tag"
    post:
      tags: [Tags]
      summary: Create a tag (or return existing by name in that namespace)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagCreateBody"
      responses:
        "201":
          description: Created or existing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tag"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/tags/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    patch:
      tags: [Tags]
      summary: Update a tag
      description: >
        Change a tag's name and/or color. Omit a field to leave it unchanged.
        Protected system tags cannot be updated.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagPatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tag"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "403":
          $ref: "#/components/responses/Forbidden"
    delete:
      tags: [Tags]
      summary: Delete a tag
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Deleted
        "404":
          $ref: "#/components/responses/NotFound"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/saved-views:
    get:
      tags: [Saved Views]
      summary: List saved views
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Saved views
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SavedView"
    post:
      tags: [Saved Views]
      summary: Create a saved view
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavedViewCreate"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedView"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/saved-views/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Saved Views]
      summary: Get one saved view
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Saved view
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedView"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Saved Views]
      summary: Replace a saved view
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavedViewCreate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedView"
    patch:
      tags: [Saved Views]
      summary: Partially update a saved view
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavedViewPatch"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedView"
    delete:
      tags: [Saved Views]
      summary: Delete a saved view
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Deleted
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/dashboard:
    get:
      tags: [Dashboard]
      summary: Aggregate task stats for the current user
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /api/v1/calendar/month:
    get:
      tags: [Calendar]
      summary: Month grid of tasks with due dates (open and completed)
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: month
          in: query
          required: false
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
          description: Year-month to display (YYYY-MM). Defaults to the current month in the user timezone.
      responses:
        "200":
          description: Calendar month grid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarMonth"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/calendar:
    get:
      tags: [Calendar]
      summary: Get or create the ICS feed token and URL
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Calendar token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarInfo"

  /api/v1/calendar/regenerate:
    post:
      tags: [Calendar]
      summary: Rotate the ICS feed token
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: New calendar token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarInfo"

  /api/v1/calendar/sync:
    post:
      tags: [Calendar]
      summary: Import due dates from an ICS export back into tasks
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [ics_file]
              properties:
                ics_file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Sync result
          content:
            application/json:
              schema:
                type: object
                properties:
                  updated:
                    type: integer

  /api/v1/export:
    get:
      tags: [Export]
      summary: Export tasks as JSON or CSV
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: format
          in: query
          schema:
            type: string
            enum: [json, csv]
            default: json
      responses:
        "200":
          description: Export payload
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
            text/csv:
              schema:
                type: string

  /api/v1/import/preview:
    post:
      tags: [Import]
      summary: Upload a CSV and preview import counts
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: >
            Staged import preview. When the CSV includes a `favorite` column, the
            response also has `Deprecation` and `Warning` headers and
            `deprecation_notice` in the body.
          headers:
            Deprecation:
              $ref: "#/components/headers/FavoriteDeprecation"
            Warning:
              $ref: "#/components/headers/FavoriteWarning"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportPreview"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/import/confirm:
    post:
      tags: [Import]
      summary: Confirm the staged CSV import
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: >
            Import completed. When the staged CSV included a `favorite` column, the
            response also has `Deprecation` and `Warning` headers and
            `deprecation_notice` in the body.
          headers:
            Deprecation:
              $ref: "#/components/headers/FavoriteDeprecation"
            Warning:
              $ref: "#/components/headers/FavoriteWarning"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/import/cancel:
    post:
      tags: [Import]
      summary: Discard the staged CSV import
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Staging cleared
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"

  /api/v1/images:
    post:
      tags: [Images]
      summary: Upload an image
      description: |
        Stores an image using the admin-configured backend (`s3` or `local`).
        S3-compatible endpoints cover AWS S3, Cloudflare R2, DigitalOcean Spaces, and MinIO.
        Local uploads are served from `/uploads/{key}`.
        Disabled when `image_hosting_provider` is unset.
        Allowed types: JPEG, PNG, GIF, WebP. Size is capped by `image_max_bytes`.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImageUpload"
        "400":
          description: Not configured, missing file, or unsupported type
        "413":
          description: File exceeds image_max_bytes
        "502":
          description: Storage backend failed; message is generic for end users

  /api/v1/admin/image-hosting/test:
    post:
      tags: [Admin]
      summary: Test image hosting configuration
      description: |
        Uploads a tiny probe image using the submitted settings (falling back to
        the saved secret key when omitted), then deletes it. The JSON `message`
        is safe for site admins and does not include provider XML or secrets.
        End-user uploads never receive this diagnostic.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminSettingsPatch"
      responses:
        "200":
          description: Probe finished (ok true or false)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImageHostingTestResult"
        "400":
          description: Provider unset or settings invalid

  /api/v1/announcements/dismiss:
    post:
      tags: [Announcements]
      summary: Dismiss the global announcement banner for this session
      security:
        - SessionCookie: []
      responses:
        "200":
          description: Dismissed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"

  /api/v1/invites:
    get:
      tags: [Invites]
      summary: List invites
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Invites
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Invite"
    post:
      tags: [Invites]
      summary: Create an invite
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
                expires_at:
                  type: string
                  format: date-time
                bypass_expiration:
                  type: boolean
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invite"

  /api/v1/invites/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    delete:
      tags: [Invites]
      summary: Delete an unused invite
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Deleted

  /api/v1/admin/settings:
    get:
      tags: [Admin]
      summary: Get site settings
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSettings"
    patch:
      tags: [Admin]
      summary: Update site settings
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminSettingsPatch"
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSettings"

  /api/v1/admin/users:
    get:
      tags: [Admin]
      summary: List users
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminUser"

  /api/v1/admin/users/{id}/ban:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Admin]
      summary: Ban a user
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Banned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"

  /api/v1/admin/users/{id}/unban:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Admin]
      summary: Unban a user
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Unbanned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OK"

  /api/v1/admin/users/{id}/username:
    parameters:
      - $ref: "#/components/parameters/ID"
    patch:
      tags: [Admin]
      summary: Set a user's username
      description: Clears username_change_available so the user is not stuck in the claim flow.
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_name]
              properties:
                user_name:
                  type: string
                  minLength: 3
                  maxLength: 32
                  pattern: '^[A-Za-z0-9_]+$'
      responses:
        "200":
          description: Username updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  id:
                    type: integer
                  user_name:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/admin/email-audit:
    get:
      tags: [Admin]
      summary: Query outbound email audit log
      description: |
        Lists recent outbound email attempts (success, failure, and not configured).
        Message bodies are never stored. Rows older than email_audit_retention_days are deleted automatically.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [sent, failed, not_configured]
        - name: trigger
          in: query
          schema:
            type: string
            enum: [password_reset, password_changed, site_invite, join_request, project_invite]
        - name: q
          in: query
          description: Case-insensitive substring match on recipient email
          schema:
            type: string
        - name: from
          in: query
          description: RFC3339 timestamp or YYYY-MM-DD (inclusive start)
          schema:
            type: string
        - name: to
          in: query
          description: RFC3339 timestamp or YYYY-MM-DD (inclusive end of day when a date)
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Audit rows, newest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailAuditList"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/admin/comment-audit:
    get:
      tags: [Admin]
      summary: Query discussion comment audit log
      description: |
        Lists previous comment bodies captured when a comment was edited, deleted, or restored.
        Site admins can restore any revision.
      security:
        - SessionCookie: []
        - BearerAuth: []
      parameters:
        - name: kind
          in: query
          schema:
            type: string
            enum: [edit, delete, restore]
        - name: q
          in: query
          description: Case-insensitive substring match on body, task title, project, or usernames
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Audit rows, newest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommentAuditList"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/admin/comment-audit/{id}/restore:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Admin]
      summary: Restore a comment to a previous version
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Restored comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskComment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/admin/join-requests:
    get:
      tags: [Admin]
      summary: List join requests
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Join requests (pending first)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/JoinRequest"

  /api/v1/admin/join-requests/{id}/approve:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Admin]
      summary: Approve a pending join request
      description: Creates or reuses a site invite and emails the requester.
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Approved
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  request:
                    $ref: "#/components/schemas/JoinRequest"
                  invite:
                    $ref: "#/components/schemas/Invite"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/admin/join-requests/{id}/deny:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Admin]
      summary: Deny a pending join request
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: Denied
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  request:
                    $ref: "#/components/schemas/JoinRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/APIError"

  /api/v1/admin/invites:
    get:
      tags: [Admin]
      summary: List all invites (admin)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "200":
          description: All invites
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Invite"
    post:
      tags: [Admin]
      summary: Create an invite (admin)
      security:
        - SessionCookie: []
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
                expires_at:
                  type: string
                  format: date-time
                bypass_expiration:
                  type: boolean
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invite"

  /api/v1/admin/invites/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    delete:
      tags: [Admin]
      summary: Delete an unused invite (admin)
      security:
        - SessionCookie: []
        - BearerAuth: []
      responses:
        "204":
          description: Deleted

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: API key
      description: API key from profile, bootstrap, or device auth
    SessionCookie:
      type: apiKey
      in: cookie
      name: session
      description: httpOnly session cookie from /api/v1/auth/login or register

  parameters:
    ID:
      name: id
      in: path
      required: true
      schema:
        type: integer
        minimum: 1

  headers:
    FavoriteDeprecation:
      description: >
        Set to `true` when the request used the deprecated `favorite` field.
        Task favoriting will be removed in API v2.
      schema:
        type: string
        example: "true"
    FavoriteWarning:
      description: >
        RFC 7234 miscellaneous persistent warning describing the favorite
        deprecation (`299 - "Task favoriting is deprecated and will be removed in API v2."`).
      schema:
        type: string
        example: '299 - "Task favoriting is deprecated and will be removed in API v2."'

  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Not authenticated
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Insufficient permissions
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: Resource is in a conflicting state
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    RateLimited:
      description: Rate limit exceeded
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    APIError:
      description: API error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

  schemas:
    Error:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
          description: Machine-readable code (e.g. invalid_request)
        message:
          type: string

    OK:
      type: object
      properties:
        ok:
          type: boolean

    TaskReorderResult:
      type: object
      properties:
        ok:
          type: boolean
        deprecation_notice:
          type: string
          description: >
            Present when reordering the deprecated favorite group (`favorite: true`).
            Favoriting will be removed in API v2.

    Health:
      type: object
      properties:
        version:
          type: string
          description: Application version (git release tag). Checkouts use the latest reachable tag; unstamped builds without git tags report "dev".
        api_enabled:
          type: boolean
        redis_ok:
          type: boolean
        mode:
          type: string
          enum: [full, api]

    SiteInfo:
      type: object
      properties:
        site_name:
          type: string
        show_changelog:
          type: boolean
        enable_global_announcement:
          type: boolean
        global_announcement_text:
          type: string
        announcement_dismissed:
          type: boolean
        enable_registration:
          type: boolean
        invite_only:
          type: boolean
        enable_join_requests:
          type: boolean
        meta_description:
          type: string
        github_oauth_configured:
          type: boolean
        image_hosting_enabled:
          type: boolean
          description: True when an admin has configured S3 or local image hosting
        image_max_bytes:
          type: integer
          format: int64
          description: Maximum upload size in bytes when image hosting is enabled
        allow_user_invites:
          type: boolean
        user_invite_limit:
          type: integer
        invite_expiration_days:
          type: integer

    User:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
          format: email
        user_name:
          type: string
        timezone:
          type: string
        items_per_page:
          type: integer
        permissions:
          type: array
          items:
            type: string
        allow_project_invites:
          type: boolean
          description: When false, the user cannot be invited to shared projects
        username_change_available:
          type: boolean
          description: When true, the user may call POST /api/v1/me/username once
        mfa_enabled:
          type: boolean
          description: When true, password login requires a TOTP or recovery code
        avatar_url:
          type: string
          description: URL to user's uploaded profile picture, or empty if unset

    MFARequired:
      type: object
      required: [mfa_required]
      properties:
        mfa_required:
          type: boolean
          enum: [true]

    MFAStatus:
      type: object
      properties:
        enabled:
          type: boolean
        recovery_codes_remaining:
          type: integer

    MFASetup:
      type: object
      properties:
        secret:
          type: string
        otpauth_url:
          type: string
          format: uri

    MFACodeRequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
          description: 6-digit TOTP or a recovery code (XXXXX-XXXXX)

    MFARecoveryCodes:
      type: object
      properties:
        recovery_codes:
          type: array
          minItems: 5
          maxItems: 5
          items:
            type: string

    UserSearchHit:
      type: object
      required: [user_name]
      properties:
        user_name:
          type: string

    UserPatch:
      type: object
      properties:
        timezone:
          type: string
        items_per_page:
          type: integer
          enum: [10, 15, 25, 50]
        allow_project_invites:
          type: boolean

    RegisterRequest:
      type: object
      required: [email, password, confirm_password, user_name]
      properties:
        email:
          type: string
        password:
          type: string
          minLength: 8
        confirm_password:
          type: string
        user_name:
          type: string
          minLength: 3
          maxLength: 32
          pattern: '^[A-Za-z0-9_]+$'
          description: Unique username; immutable after registration except by admin
        timezone:
          type: string
        invite_token:
          type: string

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
        password:
          type: string

    ChangePasswordRequest:
      type: object
      required: [current_password, new_password, confirm_password]
      properties:
        current_password:
          type: string
        new_password:
          type: string
          minLength: 8
        confirm_password:
          type: string

    APIKey:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        key_prefix:
          type: string
        created_at:
          type: string
          format: date-time
        last_used_at:
          type: string
          format: date-time
          nullable: true

    APIKeyCreated:
      allOf:
        - $ref: "#/components/schemas/APIKey"
        - type: object
          required: [key]
          properties:
            key:
              type: string
              description: Plaintext key; shown only once

    Tag:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        color:
          type: string
          maxLength: 20
          description: CSS color used for chips and board labels (for example #0d6efd)
        project_id:
          type: integer
          nullable: true
          description: Null for personal (inbox) tags
        protected:
          type: boolean
          description: True for system tags such as removed and archived

    TagCreateBody:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 50
        project_id:
          type: integer
          nullable: true
          description: Omit, null, or 0 for a personal tag; a project id for a project tag

    TagPatch:
      type: object
      description: At least one of name or color is required.
      properties:
        name:
          type: string
          maxLength: 50
        color:
          type: string
          maxLength: 20
          description: CSS color (for example #dc3545). Omit to keep the current color.

    Project:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
          maxLength: 1000
          description: Optional project details
        workflow_mode:
          type: string
          enum: [classic, kanban]
          description: Opt-in board workflow; classic keeps boolean completed only
        archived:
          type: boolean
          description: When true, the project is in the Archived section and cannot accept new tasks
        backlog_name:
          type: string
          maxLength: 60
          description: Custom name for the project's backlog system sprint (default "Backlog")
        backlog_description:
          type: string
          maxLength: 80
          description: Optional description for the project's backlog system sprint
        auto_create_next_sprint:
          type: boolean
          description: When true, automatically create the next sprint after the current dated sprint ends, if that sprint still has tasks
        auto_sprint_length_days:
          type: integer
          nullable: true
          minimum: 1
          maximum: 365
          description: Inclusive length in days of automatically created sprints
        auto_sprint_lock_days_before:
          type: integer
          nullable: true
          minimum: 0
          description: Optional days before the new sprint ends to set its lock date
        role:
          type: string
          enum: [owner, editor, viewer]
        owner_email:
          type: string
        owner_user_name:
          type: string
        owner_user_id:
          type: integer

    ProjectCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 50
        description:
          type: string
          maxLength: 1000

    ProjectPatch:
      type: object
      properties:
        name:
          type: string
          maxLength: 50
        description:
          type: string
          maxLength: 1000
        workflow_mode:
          type: string
          enum: [classic, kanban]
        backlog_name:
          type: string
          maxLength: 60
          description: Rename the project's backlog system sprint
        backlog_description:
          type: string
          maxLength: 80
          description: Update the project's backlog system sprint description
        auto_create_next_sprint:
          type: boolean
          description: Enable automatic creation of the next sprint when the current dated sprint ends (kanban only; skipped if that sprint has no tasks)
        auto_sprint_length_days:
          type: integer
          nullable: true
          minimum: 1
          maximum: 365
          description: Inclusive length in days of automatically created sprints. Required when auto_create_next_sprint is true.
        auto_sprint_lock_days_before:
          type: integer
          nullable: true
          minimum: 0
          description: Days before the automatic sprint end to set lock_date. Null clears the lock. Must be less than auto_sprint_length_days.
      description: At least one field required. Disabling kanban fails with 409 if the project has tasks.

    ProjectStatus:
      type: object
      properties:
        id:
          type: integer
        project_id:
          type: integer
        name:
          type: string
        position:
          type: integer
        is_done:
          type: boolean
        is_default:
          type: boolean
        created_at:
          type: string
          format: date-time

    ProjectSprint:
      type: object
      properties:
        id:
          type: integer
        project_id:
          type: integer
        name:
          type: string
        description:
          type: string
          maxLength: 80
          description: Optional short goal shown with the sprint name
        start_date:
          type: string
          format: date
          nullable: true
        end_date:
          type: string
          format: date
          nullable: true
          description: Inclusive range; sprints in a project must not overlap
        lock_date:
          type: string
          format: date
          nullable: true
          description: Optional. From this UTC date onward, only the project owner can add tasks
        is_active:
          type: boolean
          description: True when today's UTC date falls in start_date..end_date inclusive
        is_locked:
          type: boolean
          description: True when lock_date is set and today's UTC date is on or after lock_date
        task_count:
          type: integer
        created_at:
          type: string
          format: date-time

    TaskTimeEntry:
      type: object
      properties:
        id:
          type: integer
        task_id:
          type: integer
        user_id:
          type: integer
        minutes:
          type: integer
        note:
          type: string
        created_at:
          type: string
          format: date-time
        user_email:
          type: string
        user_name:
          type: string

    TaskComment:
      type: object
      properties:
        id:
          type: integer
        task_id:
          type: integer
        user_id:
          type: integer
        user_name:
          type: string
        avatar_url:
          type: string
          description: URL to comment author's avatar picture, or empty if unset
        body:
          type: string
          description: Empty when the comment has been deleted
        created_at:
          type: string
          format: date-time
        edited_at:
          type: string
          format: date-time
          description: Set when the comment has been edited or restored
        edited_by_user_id:
          type: integer
        edited_by_user_name:
          type: string
        deleted:
          type: boolean
        deleted_at:
          type: string
          format: date-time
        deleted_by_user_id:
          type: integer
        deleted_by_kind:
          type: string
          enum: [user, owner]
        links:
          type: array
          description: Tasks referenced in the body that the current user can access
          items:
            type: object
            properties:
              id:
                type: integer
              title:
                type: string

    TaskCommentRevision:
      type: object
      properties:
        id:
          type: integer
        comment_id:
          type: integer
        task_id:
          type: integer
        body:
          type: string
          description: Previous comment content captured before the change
        kind:
          type: string
          enum: [edit, delete, restore]
        created_at:
          type: string
          format: date-time
        edited_by_user_id:
          type: integer
        edited_by_user_name:
          type: string
        author_user_id:
          type: integer
        author_user_name:
          type: string
        task_title:
          type: string
        project_id:
          type: integer
        project_name:
          type: string
        comment_deleted:
          type: boolean
        current_body:
          type: string

    CommentAuditList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/TaskCommentRevision"
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    ProjectMember:
      type: object
      properties:
        user_id:
          type: integer
        email:
          type: string
        user_name:
          type: string
        role:
          type: string
          enum: [owner, editor, viewer]
        created_at:
          type: string
          format: date-time

    ProjectInvite:
      type: object
      properties:
        id:
          type: integer
        project_id:
          type: integer
        email:
          type: string
        user_name:
          type: string
        role:
          type: string
          enum: [editor, viewer]
        expires_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        project_name:
          type: string
        inviter_email:
          type: string
        inviter_user_name:
          type: string

    ProjectInviteCreate:
      type: object
      required: [username, role]
      properties:
        username:
          type: string
          description: Unique username of the user to invite
          minLength: 3
          maxLength: 32
        role:
          type: string
          enum: [editor, viewer]

    RoleBody:
      type: object
      required: [role]
      properties:
        role:
          type: string
          enum: [editor, viewer]

    ProjectEvent:
      type: object
      properties:
        id:
          type: integer
        project_id:
          type: integer
        actor_user_id:
          type: integer
        actor_email:
          type: string
        actor_user_name:
          type: string
        event_type:
          type: string
        source:
          type: string
          enum: [project, task]
        task_id:
          type: integer
        label:
          type: string
        metadata:
          type: object
          additionalProperties: true
        created_at:
          type: string
          format: date-time

    ShareLink:
      type: object
      properties:
        id:
          type: integer
        token:
          type: string
        url:
          type: string
        scope_type:
          type: string
          enum: [project]
        scope_id:
          type: integer
        expires_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time

    ShareLinkCreate:
      type: object
      required: [scope_type, scope_id]
      properties:
        scope_type:
          type: string
          enum: [project]
        scope_id:
          type: integer
        expires_at:
          type: string
          format: date-time

    ShareLinkView:
      type: object
      properties:
        scope_type:
          type: string
        scope_id:
          type: integer
        tasks:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              title:
                type: string
              completed:
                type: boolean
              due_date:
                type: string
              priority:
                type: integer
              project:
                type: string
              description:
                type: string
              status_name:
                type: string
              tags:
                type: array
                items:
                  $ref: "#/components/schemas/Tag"

    NameBody:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 50

    Task:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        completed:
          type: boolean
        due_date:
          type: string
        project_id:
          type: integer
          nullable: true
        project:
          type: string
        priority:
          type: integer
          minimum: 0
          maximum: 3
        favorite:
          type: boolean
          deprecated: true
          description: >
            Whether the task is starred. Deprecated; will be removed in API v2.
            Still returned. Requests that set this field receive `Deprecation` and
            `Warning` headers plus `deprecation_notice` in the JSON body.
        position:
          type: integer
        parent_id:
          type: integer
          nullable: true
          description: Parent task id; null for root tasks. One level of nesting only.
        child_count:
          type: integer
        children_completed:
          type: integer
        children:
          type: array
          items:
            $ref: "#/components/schemas/Task"
          description: Direct subtasks when listing or fetching a root task
        tags:
          type: array
          items:
            $ref: "#/components/schemas/Tag"
        created_at:
          type: string
        modified_at:
          type: string
        deprecation_notice:
          type: string
          description: >
            Present on create/update responses that included the deprecated
            `favorite` field. Favoriting will be removed in API v2.
        status_id:
          type: integer
          nullable: true
        status_name:
          type: string
        estimate_points:
          type: integer
          nullable: true
        time_spent_minutes:
          type: integer
        project_workflow:
          type: string
          enum: [classic, kanban]
        claimed_by:
          type: integer
          nullable: true
          description: User id of the claimer for kanban tasks; null when unclaimed
        claimed_by_name:
          type: string
          description: Display name of the claimer
        sprint_id:
          type: integer
          nullable: true
          description: Kanban sprint assignment; null when the task is in the backlog
        sprint_name:
          type: string
        parent_title:
          type: string
          description: Title of the parent task when this item is a subtask
        github:
          $ref: "#/components/schemas/TaskGitHubIssue"

    GitHubConnection:
      type: object
      properties:
        connected:
          type: boolean
        github_login:
          type: string
        auth_method:
          type: string
          enum: [oauth, pat]
        connected_at:
          type: string
          format: date-time

    ProjectGitHubRepo:
      type: object
      properties:
        linked:
          type: boolean
        owner:
          type: string
        repo:
          type: string
        full_name:
          type: string
        html_url:
          type: string
        repo_id:
          type: integer
          format: int64
        linked_by_user_id:
          type: integer
        webhook_secret:
          type: string
          description: Present for project owners only
        linked_at:
          type: string
          format: date-time

    TaskGitHubIssue:
      type: object
      properties:
        issue_number:
          type: integer
        issue_id:
          type: integer
          format: int64
        issue_url:
          type: string
        issue_state:
          type: string
        issue_title:
          type: string
        last_sync_error:
          type: string

    Notification:
      type: object
      properties:
        id:
          type: integer
        type:
          type: string
        title:
          type: string
        body:
          type: string
        project_id:
          type: integer
          nullable: true
        task_id:
          type: integer
          nullable: true
        project_name:
          type: string
        actor_name:
          type: string
        read_at:
          type: string
          nullable: true
        created_at:
          type: string

    NotificationList:
      type: object
      properties:
        notifications:
          type: array
          items:
            $ref: "#/components/schemas/Notification"
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer
        unread_count:
          type: integer

    TaskList:
      type: object
      properties:
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/Task"
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer
        total_pages:
          type: integer
        completed_count:
          type: integer
        incomplete_count:
          type: integer

    TaskCreate:
      type: object
      required: [title]
      properties:
        title:
          type: string
        description:
          type: string
        due_date:
          type: string
        project_id:
          type: integer
          nullable: true
        parent_id:
          type: integer
          nullable: true
          description: Root task id to nest under; must itself have no parent
        priority:
          type: integer
        completed:
          type: boolean
        favorite:
          type: boolean
          deprecated: true
          description: >
            Optional (default false). Deprecated; will be removed in API v2.
            Including this field still stars or unstars the task and returns
            `Deprecation`/`Warning` headers plus `deprecation_notice`.
        tag_ids:
          type: array
          items:
            type: integer
        status_id:
          type: integer
          nullable: true
        estimate_points:
          type: integer
          nullable: true
        sprint_id:
          type: integer
          nullable: true
          description: Assign the task to a sprint in its kanban project; 0 or omit leaves it in the backlog. After a sprint lock date, only the project owner can assign tasks to that sprint.

    TaskPatch:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        due_date:
          type: string
        clear_due_date:
          type: boolean
        project_id:
          type: integer
          nullable: true
          description: >
            Set a project id, or null/0 to move the task to the inbox. Sending the
            current project is a no-op. Only the source project owner can change
            project_id on a task that already belongs to a project.
        parent_id:
          type: integer
          nullable: true
          description: Set to a root task id to nest, or null to promote to root
        priority:
          type: integer
        completed:
          type: boolean
        favorite:
          type: boolean
          deprecated: true
          description: >
            Deprecated; will be removed in API v2. Including this field still
            stars or unstars the task and returns `Deprecation`/`Warning` headers
            plus `deprecation_notice`.
        tag_ids:
          type: array
          items:
            type: integer
        status_id:
          type: integer
          nullable: true
        estimate_points:
          type: integer
          nullable: true
        sprint_id:
          type: integer
          nullable: true
          description: Set a sprint in the task's kanban project. null or 0 moves the task to the backlog; omit to leave the assignment unchanged. After a sprint lock date, only the project owner can assign tasks to that sprint.

    TaskReorder:
      type: object
      required: [task_ids, favorite]
      properties:
        task_ids:
          type: array
          items:
            type: integer
        favorite:
          type: boolean
          deprecated: true
          description: >
            Which group is being reordered. Deprecated; favorite grouping will be
            removed in API v2. Still required. `favorite: true` returns
            `Deprecation`/`Warning` headers plus `deprecation_notice`.
        parent_id:
          type: integer
          nullable: true
          description: When set, reorder siblings under this parent; when null/omitted, reorder roots
        page:
          type: integer
        per_page:
          type: integer
        project:
          type: string

    TaskBulk:
      type: object
      required: [action, task_ids]
      properties:
        action:
          type: string
          enum:
            - complete
            - incomplete
            - delete
            - move_project
            - add_tag
            - remove_tag
            - set_priority
            - set_due_date
            - set_status
            - set_sprint
        task_ids:
          type: array
          items:
            type: integer
          maxItems: 100
        project_id:
          type: integer
          nullable: true
        tag_id:
          type: integer
        priority:
          type: integer
          minimum: 0
          maximum: 3
        due_date:
          type: string
          description: YYYY-MM-DD; empty string clears
        status_id:
          type: integer
          description: Kanban column status id for set_status
        sprint_id:
          type: integer
          nullable: true
          description: Sprint id for set_sprint; 0 or null moves tasks to the project backlog

    BulkResult:
      type: object
      properties:
        ok:
          type: boolean
        affected:
          type: integer
        action:
          type: string
        undo_token:
          type: string
        expires_in:
          type: integer

    ImportPreview:
      type: object
      properties:
        preview:
          type: array
          items:
            type: object
            properties:
              title:
                type: string
              project:
                type: string
              due_date:
                type: string
              tags:
                type: string
        would_import:
          type: integer
        would_skip:
          type: integer
        total_rows:
          type: integer
        deprecation_notice:
          type: string
          description: >
            Present when the CSV includes a `favorite` column. Favoriting will
            be removed in API v2.

    ImportResult:
      type: object
      properties:
        imported:
          type: integer
        skipped:
          type: integer
        deprecation_notice:
          type: string
          description: >
            Present when the staged CSV included a `favorite` column. Favoriting
            will be removed in API v2.

    DeleteResult:
      type: object
      properties:
        ok:
          type: boolean
        undo_token:
          type: string
        expires_in:
          type: integer

    TaskEvent:
      type: object
      properties:
        id:
          type: integer
        task_id:
          type: integer
        event_type:
          type: string
        label:
          type: string
        metadata:
          type: object
          additionalProperties: true
        created_at:
          type: string
          format: date-time
        actor_user_id:
          type: integer
        actor_user_name:
          type: string
        actor_email:
          type: string

    LiveEvent:
      type: object
      properties:
        type:
          type: string
          enum:
            - task.created
            - task.updated
            - task.deleted
            - task.reordered
            - project.updated
        task_id:
          type: integer
        project_id:
          type: integer
        actor_id:
          type: integer
        origin:
          type: string
        timestamp:
          type: string
          format: date-time

    SavedViewFilter:
      type: object
      properties:
        project:
          type: string
        status:
          type: string
        due:
          type: string
        completed:
          type: string
        priority:
          type: string
        tag:
          type: string
        sort:
          type: string
        search:
          type: string

    SavedView:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        filter:
          $ref: "#/components/schemas/SavedViewFilter"
        sort_order:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    SavedViewCreate:
      type: object
      required: [name, filter]
      properties:
        name:
          type: string
        filter:
          $ref: "#/components/schemas/SavedViewFilter"
        sort_order:
          type: integer

    SavedViewPatch:
      type: object
      properties:
        name:
          type: string
        filter:
          $ref: "#/components/schemas/SavedViewFilter"
        sort_order:
          type: integer

    DeviceCodeResponse:
      type: object
      properties:
        device_code:
          type: string
        user_code:
          type: string
        verification_uri:
          type: string
        verification_uri_complete:
          type: string
        expires_in:
          type: integer
        interval:
          type: integer
        redirect_uri:
          type: string
          description: Echo of the validated native deep link, when requested

    DeviceStatus:
      type: object
      properties:
        user_code:
          type: string
        client_name:
          type: string
        status:
          type: string
        redirect_uri:
          type: string
          description: Native deep link to open after a decision (when requested at code issuance)

    DeviceDecisionResponse:
      type: object
      properties:
        ok:
          type: boolean
        status:
          type: string
          description: approved or denied
        redirect_uri:
          type: string
          description: |
            Deep link for the browser to open. On approve includes status=approved;
            on deny includes error=access_denied. Omitted when no redirect_uri was requested.

    DashboardStats:
      type: object
      properties:
        overdue_count:
          type: integer
        due_today_count:
          type: integer
        due_this_week_count:
          type: integer
          description: Incomplete tasks due from today through end of the current calendar week (excludes overdue)
        completed_this_week:
          type: integer
        completed_this_month:
          type: integer
        streak_days:
          type: integer
        by_project:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              count:
                type: integer
        by_priority:
          type: array
          items:
            type: object
            properties:
              priority:
                type: integer
              label:
                type: string
              count:
                type: integer
        completions_last_7_days:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
              count:
                type: integer

    CalendarInfo:
      type: object
      properties:
        token:
          type: string
        feed_url:
          type: string

    CalendarMonthTask:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        due:
          type: string
        priority:
          type: integer
        project_name:
          type: string
        completed:
          type: boolean

    CalendarMonthCell:
      type: object
      properties:
        date:
          type: string
        day:
          type: integer
        in_month:
          type: boolean
        is_today:
          type: boolean
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/CalendarMonthTask"

    CalendarMonth:
      type: object
      properties:
        year_month:
          type: string
        month_label:
          type: string
        prev_month:
          type: string
        next_month:
          type: string
        today_month:
          type: string
        year:
          type: integer
        weeks:
          type: array
          items:
            type: array
            items:
              $ref: "#/components/schemas/CalendarMonthCell"

    Invite:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        token:
          type: string
        used:
          type: boolean
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          nullable: true
        created_by:
          type: integer
          nullable: true
        creator_user_name:
          type: string
        creator_email:
          type: string
        status:
          type: string
          enum: [pending, used, expired]

    JoinRequest:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        message:
          type: string
        status:
          type: string
          enum: [pending, approved, denied]
        created_at:
          type: string
          format: date-time
        invite_id:
          type: integer
        reviewed_at:
          type: string
          format: date-time
        reviewed_by:
          type: integer
        invite_token:
          type: string

    AdminSettings:
      type: object
      properties:
        site_name:
          type: string
        default_timezone:
          type: string
        show_changelog:
          type: boolean
        site_version:
          type: string
          description: Read-only. Application version (git release tag from the running process). Not stored in site settings.
        enable_registration:
          type: boolean
        invite_only:
          type: boolean
        enable_join_requests:
          type: boolean
        meta_description:
          type: string
        enable_global_announcement:
          type: boolean
        global_announcement_text:
          type: string
        enable_api:
          type: boolean
        allow_user_invites:
          type: boolean
        user_invite_limit:
          type: integer
        invite_expiration_days:
          type: integer
        email_provider:
          type: string
          description: none, mailgun, or smtp
        email_from_address:
          type: string
        email_from_name:
          type: string
        email_mailgun_domain:
          type: string
        email_mailgun_api_key_set:
          type: boolean
        email_smtp_host:
          type: string
        email_smtp_port:
          type: integer
        email_smtp_username:
          type: string
        email_smtp_password_set:
          type: boolean
        email_smtp_tls:
          type: boolean
        email_audit_retention_days:
          type: integer
          minimum: 1
          maximum: 90
          description: How many days to keep outbound email audit rows (default 7)
        image_hosting_provider:
          type: string
          description: none, s3, or local
        image_max_bytes:
          type: integer
          format: int64
          minimum: 65536
          maximum: 52428800
          description: Maximum image size in bytes (default 5 MiB)
        image_s3_endpoint:
          type: string
          description: S3-compatible API endpoint (AWS, Cloudflare R2, DigitalOcean Spaces, MinIO)
        image_s3_region:
          type: string
        image_s3_bucket:
          type: string
        image_s3_access_key:
          type: string
        image_s3_secret_key_set:
          type: boolean
        image_s3_public_url:
          type: string
          description: Public CDN or bucket URL used to form image URLs
        image_s3_force_path_style:
          type: boolean
          description: Use path-style URLs (recommended for R2, Spaces, and MinIO)
        image_local_path:
          type: string
          description: Directory on the server for local uploads (default data/uploads)

    AdminSettingsPatch:
      type: object
      properties:
        site_name:
          type: string
        default_timezone:
          type: string
        show_changelog:
          type: boolean
        enable_registration:
          type: boolean
        invite_only:
          type: boolean
        enable_join_requests:
          type: boolean
        meta_description:
          type: string
        enable_global_announcement:
          type: boolean
        global_announcement_text:
          type: string
        enable_api:
          type: boolean
        allow_user_invites:
          type: boolean
        user_invite_limit:
          type: integer
        invite_expiration_days:
          type: integer
        email_provider:
          type: string
        email_from_address:
          type: string
        email_from_name:
          type: string
        email_mailgun_domain:
          type: string
        email_mailgun_api_key:
          type: string
          description: Omit to keep; non-empty to replace; empty string to clear
        email_smtp_host:
          type: string
        email_smtp_port:
          type: integer
        email_smtp_username:
          type: string
        email_smtp_password:
          type: string
          description: Omit to keep; non-empty to replace; empty string to clear
        email_smtp_tls:
          type: boolean
        email_audit_retention_days:
          type: integer
          minimum: 1
          maximum: 90
        image_hosting_provider:
          type: string
        image_max_bytes:
          type: integer
          format: int64
        image_s3_endpoint:
          type: string
        image_s3_region:
          type: string
        image_s3_bucket:
          type: string
        image_s3_access_key:
          type: string
        image_s3_secret_key:
          type: string
          description: Omit to keep; non-empty to replace; empty string to clear
        image_s3_public_url:
          type: string
        image_s3_force_path_style:
          type: boolean
        image_local_path:
          type: string

    AdminUser:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        user_name:
          type: string
        is_banned:
          type: boolean
        interval:
          type: integer

    EmailAuditEntry:
      type: object
      properties:
        id:
          type: integer
        created_at:
          type: string
          format: date-time
        trigger:
          type: string
        to_email:
          type: string
        status:
          type: string
          enum: [sent, failed, not_configured]
        error:
          type: string
        provider:
          type: string

    EmailAuditList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/EmailAuditEntry"
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    ImageUpload:
      type: object
      properties:
        url:
          type: string
        content_type:
          type: string
        size:
          type: integer
        filename:
          type: string
        key:
          type: string
    ImageHostingTestResult:
      type: object
      properties:
        ok:
          type: boolean
        message:
          type: string
        public_url_ok:
          type: boolean
